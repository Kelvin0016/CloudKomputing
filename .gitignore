# Terraform
*.tfstate
*.tfstate.*
.terraform/
crash.log
*.tfvars
# Note: .terraform.lock.hcl is intentionally NOT ignored — it should be committed

# Terraform-generated key pair (never commit private keys)
*.pem

# Lambda build artifact (regenerated by Terraform)
lambda.zip

# OS files
.DS_Store
Thumbs.db
